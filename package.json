{
  "name": "landis",
  "description": "A Vue.js project",
  "version": "1.0.0",
  "author": "",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "npm install -g yarn && cd app/backend && yarn && yarn start & cd app/frontend && yarn && yarn build"
  }
}
